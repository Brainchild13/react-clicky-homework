import React from "react";
import "./jumbotron.css";

const Jumbotron = () => (
    <header className = "header">
        <h1>Starwars Clicky Image Game</h1>
        <h2>Click on any image to earn a point. Make sure not to click any image more than once and try to click all 12 images without clicking the same one twice and you win!</h2>
    </header>
);

export default Jumbotron;